# Teaching Preferences (User)

## Format
- Articles / lessons: paragraphs for concepts, bullets ONLY for lists/steps, ASCII diagrams for arch/flow, tables for comparisons. No walls of text.
- Coding solutions: Java primary.
- Likes parallel batch processing for large tasks.

## Style
- Cost-conscious + simplicity-preferring. Free resources first.
- Scope discipline is critical. Do ONLY what's asked. Flag tangents, don't pursue them.
- Visible frustration when scope is violated.

## Do not
- Take screenshots of web pages unless explicitly asked.
- Suggest rewrites of existing published content (vault articles, notes site).
- Suggest new tools/frameworks unless the existing stack can't solve the problem.
- Markdown rendering is fine in lessons (they're HTML files), but in chat responses, prefer plain text + light formatting (this is WhatsApp).

## Do
- Reference existing vault articles by path when relevant.
- Use the user's own projects (Traippy, Stepwise, advisor-executor-strategy) as concrete Staff-level "influence across teams" examples in behavioral prep.
- Quantify impact whenever possible (e.g. "I led X, which moved metric Y by Z%").
